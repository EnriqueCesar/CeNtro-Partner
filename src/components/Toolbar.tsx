import { RotateCcw, Upload } from 'lucide-react'
import { useData } from './DataContext'
import type { Period, Pillar } from '../types'
export function Toolbar(){const{period,setPeriod,upload,data,dm,setDm,pillar,setPillar,dms,resetFilters}=useData();return <section className="card mb-6"><div className="mb-4 flex flex-col gap-2 md:flex-row md:items-center md:justify-between"><div><p className="eyebrow">Archivo activo</p><p className="font-semibold text-slate-900">{data?.fileName??'Sin archivo'}</p></div><button type="button" className="ghost-button" onClick={resetFilters}><RotateCcw size={16}/> Limpiar filtros</button></div><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><label className="filter-label">Mes<select value={period} onChange={e=>setPeriod(e.target.value as Period)} className="control">{['ene','feb','mar','abr','may','jun','YTD'].map(p=><option key={p}>{p}</option>)}</select></label><label className="filter-label">DM<select value={dm} onChange={e=>setDm(e.target.value)} className="control"><option>Todos</option>{dms.map(v=><option key={v}>{v}</option>)}</select></label><label className="filter-label">Pilar<select value={pillar} onChange={e=>setPillar(e.target.value as Pillar)} className="control">{['Todos','Partner','Cliente','Negocio'].map(v=><option key={v}>{v}</option>)}</select></label><label className="filter-label">Archivo local<span className="control flex cursor-pointer items-center justify-center gap-2"><Upload size={17}/> Cargar Excel<input type="file" accept=".xlsx,.xls" className="hidden" onChange={e=>e.target.files?.[0]&&void upload(e.target.files[0])}/></span></label></div></section>}
