import { CheckCircle2, AlertTriangle, XCircle } from 'lucide-react';import { Toolbar } from '../components/Toolbar';import { useData } from '../components/DataContext'
export function AuditPage(){const{data,loading,error}=useData();if(loading)return <p>Cargando…</p>;return <><Toolbar/>{error&&<div className="error">{error}</div>}<div className="card"><h3 className="section-title">Auditoría del archivo</h3><div className="space-y-3">{data?.audit.map((a,i)=>{const Icon=a.level==='ok'?CheckCircle2:a.level==='warning'?AlertTriangle:XCircle;return <div key={i} className="flex items-center gap-3 rounded-xl border p-3"><Icon size={20} className={a.level==='ok'?'text-emerald-600':a.level==='warning'?'text-amber-600':'text-red-600'}/><span>{a.message}</span></div>})}</div></div></>}
