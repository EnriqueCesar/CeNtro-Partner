import { Toolbar } from '../components/Toolbar'
import { useData } from '../components/DataContext'
export function RankingPage(){const{stores,loading,error}=useData();if(loading)return <div className="loading">Calculando ranking…</div>;if(error)return <div className="error">{error}</div>;return <><Toolbar/><div className="card overflow-x-auto"><div className="section-heading"><div><p className="eyebrow">Resultado dinámico</p><h3 className="section-title">Ranking Regional</h3></div><span className="summary-chip">{stores.length} tiendas</span></div><table><thead><tr><th>Rank</th><th>CeCo</th><th>Tienda</th><th>DM</th><th>Indicadores aplicables</th><th>Cumplimiento</th></tr></thead><tbody>{stores.map(s=><tr key={s.CeCo}><td className="rank">#{s.rank}</td><td>{s.CeCo}</td><td className="font-semibold">{s.Tienda}</td><td>{s.DM}</td><td>{s.applicable}</td><td><span className={`badge ${s.compliance>=.8?'ok':s.compliance>=.6?'warn':'bad'}`}>{(s.compliance*100).toFixed(1)}%</span></td></tr>)}</tbody></table></div></>}
